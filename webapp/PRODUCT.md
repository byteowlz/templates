# PRODUCT.md — {{project_name}}

(Impeccable durable context. `impeccable init` owns this file — it seeds it,
subsequent design work keeps it current.)

## Product

- **What**: one focused tool. Replace this stub with: what it does, for whom,
  and the one job that defines success.
- **Mode**: Operate (a workbench — the visitor completes a task; scanability
  and the real usage scene outrank expression).
- **Users**: byteowlz agents + Tommy, at a desk, on desktop browsers via the
  tailnet; mobile mainly for glance/status.

## Success

- A returning user completes the core job in seconds, without relearning.

## Non-goals

- Multi-tenant product surface, marketing site, theming UI of its own.

"$schema" = "https://raw.githubusercontent.com/byteowlz/schemas/refs/heads/main/rust-cli/rust-cli.config.schema.json"

# Configuration for rust-cli.
# Copy this file to $XDG_CONFIG_HOME/rust-cli/config.toml and adjust as needed.

profile = "default"

[logging]
level = "info"

[runtime]
timeout = 60
fail_fast = true

[paths]
